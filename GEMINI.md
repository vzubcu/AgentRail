@AGENTS.md

## Gemini Notes

- Use `AGENTS.md` as the canonical repo guide.
- Use `docs/ai-memory/` for deeper project orientation before broad code changes.
- Prefer `graphify query`, `graphify explain`, and `graphify path` before broad repo searches.
- Run `graphify update .` after relevant code edits; git hooks do not replace a manual refresh for uncommitted work.