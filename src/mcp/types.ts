/** * Shared types for the AgentRail MCP server bridge. * * The MCP tool surface uses snake_case parameter names (MCP JSON-RPC * convention), while the internal HTTP client maps them to camelCase * request bodies for the AgentRail gateway. */ export interface McpEnvConfig { /** Base URL of the AgentRail gateway. Defaults to http://localhost:42424 */ agentrailUrl: string; /** Optional gateway auth key sent as Authorization: Bearer <key>. */ agentrailApiKey?: string; /** Optional outbound request timeout in milliseconds. Defaults to 30000. */ timeoutMs: number; } /** OpenAI-compatible chat message shape accepted by the gateway. */ export interface ChatMessage { role: 'system' | 'user' | 'assistant' | 'tool'; content: string | Array<Record<string, unknown>>; name?: string; tool_call_id?: string; } /** Tool input for chat_completions. */ export interface ChatCompletionsArgs { model: string; /** MCP delivers untyped JSON, so we accept unknown[] and validate at runtime. */ messages: unknown[]; temperature?: number; max_tokens?: number; top_p?: number; stream?: boolean; /** Optional provider hint forwarded to AgentRail (e.g. "openrouter"). */ provider?: string; } /** Tool input for check_provider. */ export interface CheckProviderArgs { provider: string; } /** Tool input for list_streams (internal/reserved). */ export interface ListStreamsArgs { limit?: number; }